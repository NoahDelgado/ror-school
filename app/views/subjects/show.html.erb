<% content_for :title, "Subject Details" %>

<section class="section">
  <div class="container">
    <div class="level mb-5">
      <div class="level-left">
        <div class="level-item">
          <h1 class="title">Subject Details</h1>
        </div>
      </div>
      <div class="level-right">
        <div class="level-item">
          <%= link_to subjects_path, class: "button is-light" do %>
            <span class="icon">
              <i class="fas fa-arrow-left"></i>
            </span>
            <span>Back to Subjects</span>
          <% end %>
        </div>
      </div>
    </div>

    <div class="card">
      <header class="card-header">
        <p class="card-header-title">
          <span class="icon-text">
            <span class="icon">
              <i class="fas fa-book"></i>
            </span>
            <span><%= @subject.name %></span>
          </span>
        </p>
      </header>
      <div class="card-content">
        <div class="content">
          <p class="has-text-weight-bold">Name: <%= @subject.name %></p>
        </div>
      </div>
      <footer class="card-footer">
        <% if current_person&.is_a?(Dean) %>
          <%= link_to edit_subject_path(@subject), class: "card-footer-item" do %>
            <span class="icon-text">
              <span class="icon">
                <i class="fas fa-edit"></i>
              </span>
              <span>Edit</span>
            </span>
          <% end %>
          
          <%= button_to subject_path(@subject), method: :delete, class: "card-footer-item button is-white has-text-danger", data: { turbo_confirm: "Are you sure you want to delete this subject?" } do %>
            <span class="icon-text">
              <span class="icon">
                <i class="fas fa-trash"></i>
              </span>
              <span>Delete</span>
            </span>
          <% end %>
        <% end %>
      </footer>
    </div>

    <% if current_person&.is_a?(Dean) %>
      <div class="mt-5">
        <h2 class="subtitle">Courses Using This Subject</h2>
        <% courses = Course.where(subject_id: @subject.id) %>
        
        <% if courses.any? %>
          <div class="table-container">
            <table class="table is-fullwidth is-hoverable">
              <thead>
                <tr>
                  <th>Class</th>
                  <th>Teacher</th>
                  <th>Period</th>
                  <th>Schedule</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% courses.each do |course| %>
                  <tr>
                    <td><%= course.school_class&.name || "Unknown class" %></td>
                    <td><%= course.person&.full_name || "No teacher assigned" %></td>
                    <td><%= course.moment&.display_name || "Unknown period" %></td>
                    <td>
                      <span class="icon-text">
                        <span class="icon">
                          <i class="fas fa-calendar-day"></i>
                        </span>
                        <span>
                          <%= [
                            ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"][course.week_day],
                            course.start_at&.strftime("%H:%M"),
                            course.end_at&.strftime("%H:%M")
                          ].compact.join(" | ") %>
                        </span>
                      </span>
                    </td>
                    <td>
                      <div class="buttons are-small">
                        <%= link_to course_path(course), class: "button is-info" do %>
                          <span class="icon">
                            <i class="fas fa-eye"></i>
                          </span>
                          <span>View</span>
                        <% end %>
                      </div>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        <% else %>
          <div class="notification is-info is-light">
            <p>This subject is not used in any courses yet.</p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</section>
